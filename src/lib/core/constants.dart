import 'package:flutter/material.dart';

// Default padding value used throughout the app
const double kDefaultPadding = 16.0;

// You can add other constants here as needed
// e.g., API keys (use environment variables for sensitive data!),
// default durations, common colors not part of the theme, etc.